// string, number, undefined, null, boolean, symbol
const nome = 'Franklin'; // string
const nome1 = "Franklin"; // string
const nome2 = `Franklin`; // string
const num1 = 12; // number
const num2 = 12.34; // number

let nomeAluno; // undefined -> não aponta pata nenhum local na memória
const sobrenomeAluno = null; // nulo -> não aponta pata nenhum local na memória
const aprovado = false; // boolean = true, false

