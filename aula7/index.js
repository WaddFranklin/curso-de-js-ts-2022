// constantes não podem ser declaradas sem ser inicializadas

// não podem ter seu valor modificado

const nome = 'João';

const primeiroNumero = 5;
const segundoNumero = 10;
const resultado = primeiroNumero * segundoNumero;

console.log(nome, resultado, typeof primeiroNumero);